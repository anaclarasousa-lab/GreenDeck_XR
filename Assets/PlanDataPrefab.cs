using UnityEngine;

public class PlanDataPrefab : MonoBehaviour
{
    public PlantData plantData; 
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
